<?php
// Tipe Data String
$nama_produk = "Laptop Gaming ASUS";
var_dump($nama_produk);

// Tipe Data Integer
$stok_barang = 15;
var_dump($stok_barang);

// Tipe Data Float/Double
$berat_barang = 2.3; // dalam kilogram
var_dump($berat_barang);

// Tipe Data Boolean
$garansi_aktif = true;
var_dump($garansi_aktif);

// Tipe Data Array
$spesifikasi = ["Processor i7", "RAM 16GB", "SSD 512GB"];
var_dump($spesifikasi);
?>